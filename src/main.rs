#![allow(dead_code, unused_imports)]
mod union_ex;
mod slices;
mod strings;
use union_ex::unions;
use slices::slice;
use strings::strings;

fn main() {
    // Unions example
    //unions();

    //slice();

    strings();
}
